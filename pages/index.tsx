import Header from '@/components/Header';
import { FC } from 'react';

interface HomeProps {

}

const Home: FC<HomeProps> = ({ }) => {
  return <div>
    <Header />
  </div>;
};

export default Home;